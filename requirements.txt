# SalesPilot dependencies.
# The deterministic kernel and SQLite persistence use the Python standard
# library. The current backend service also uses the dependencies below.
# Install: py -3 -m pip install -r requirements.txt

# Test runner (the suite itself also runs with stdlib unittest)
pytest>=8.0

# HTTP service layer (`py -3 -m backend --serve`)
fastapi>=0.115
uvicorn>=0.30

# Test client for the API tests
httpx>=0.27

# ---- backend/ (the rebuild) ---------------------------------------------
# Agent runtime: the tool-calling loop and structured outputs whose schemas are
# generated from backend/domain/enums.py. Confined to backend/agent/ — see
# docs/v0.0/backend/backend-plan.md §4 and backend/tests/test_architecture.py, which fails the
# build if it leaks into any other package.
#
# The `[openai]` extra pulls the OpenAI client, used for ANY OpenAI-compatible
# endpoint including the organiser gateway. backend/domain/ and backend/kernel/
# stay standard-library only and run with none of this installed.
pydantic-ai-slim[openai]==2.46.0
