# SalesPilot dependencies.
# The decision engine, rule-based RAG, SQLite persistence, analytics, CLI and
# tests (unittest) run on the Python standard library only (Python >= 3.10).
# Install: py -3 -m pip install -r requirements.txt

# Test runner (the suite itself also runs with stdlib unittest)
pytest>=8.0

# HTTP service layer (`py -3 run.py --serve`)
fastapi>=0.115
uvicorn>=0.30

# Test client for the API tests
httpx>=0.27

# Optional: real LLM reply generation (`--llm`, SALESPILOT_LLM=openai)
# openai>=1.40
